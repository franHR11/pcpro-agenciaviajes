extends ./layout/index

block contenido
            h1.text-center.mt-5= pagina 
            blockquote.text-center Lee sobre nuestros clientes y sus experiencias 
            include ./layout/bloque_testimoniales
            
            .row 
                .col-md-12
                    h2.d-block.w-100.text-center Agrega un Testimonial
                    
                    if(errores)
                        each error in errores
                            .alert.col.alert-danger.text-center= error.mensaje
            .row.justify-content-center.col 
                .col-md-8   
                    form(action="/testimoniales" method="POST" onsubmit="return validateForm()") 
                        .form-group  
                            label(for="nombre") Nombre 
                            input#nombre(type="text", name="nombre" placeholder="Tu Nombre" class="form-control", value=nombre)   
                        .form-group 
                            label(for="correo") Correo Electronico 
                            input#correo(type="email", name="correo" placeholder="Tu Correo" class="form-control", value=correo)
                        .form-group 
                            label(for="mensaje") Mensaje 
                            textarea#mensaje(class="form-control", name="mensaje", placeholder="Tu Mensaje", rows=3, value=mensaje)
                        input.btn.btn-success.btn-block(type="submit" value="Enviar")

       
